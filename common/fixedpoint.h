/* fake_fixedpoint.h - Minimal fixed point definitions for standalone
 * compilation */
#ifndef FIXEDPOINT_H
#define FIXEDPOINT_H

/*
 * The bypass model uses standard types, so we likely don't need
 * complex fixed-point macros here. An empty definition or minimal
 * typdefs are usually sufficient to satisfy the #include.
 */

#endif
